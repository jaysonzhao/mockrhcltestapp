# Server port
server.port=8080

# Application name
spring.application.name=test-pages-app

# Logging configuration
logging.level.root=INFO
logging.level.com.example.testpagesapp=DEBUG

# Allow custom status codes
server.error.include-message=always